# Python bytecode and temporary files
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.Python
.ipynb_checkpoints/ # Jupyter Notebook checkpoints
profile.prof

# Virtual environment
# This is the most common name for a venv directory
venv/
.venv/
env/
.env/

# IDE-specific files
.vscode/        # VS Code settings (optional, only ignore if not shared)
.idea/          # IntelliJ/PyCharm project files
*.sublime-project
*.sublime-workspace
.DS_Store       # macOS specific
Thumbs.db       # Windows specific

# PyInstaller build artifacts
# These directories are created by PyInstaller during the build process
build/
dist/
*.spec          # The PyInstaller spec file itself (often kept, but can be ignored if you prefer to regenerate)
                # NOTE: We customized the .spec file, so you might *want* to commit it.
                # If you want to commit it, remove this line or comment it out.
                # For this project, it's customized and should probably be committed.

# Downloaded files and settings (generated by the application at runtime)
downloads/      # Your application's default download directory
settings.json   # Your application's settings file
*.log           # Any log files generated by the app (e.g., download logs)

# Operating System Files
.project
.pydevproject

# Other common temporary files
*~              # Backup files
*.bak           # Backup files
*.swp           # Vim swap files
*.swo           # Vim swap files
.fuse_hidden*
.nfs*